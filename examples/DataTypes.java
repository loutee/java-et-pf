public class DataTypes {
	public static void main(String[] args) {
		char letter = 'A';
		boolean done = false;
		int radius = 10;
		byte red = 127; // [-127, 127] or [0, 255]
		short age = 21; // smaller than int
		long population = 313459000;
		float price = 10.59f; // note that the f must be at the end
		double circleArea = Math.PI*radius*radius;
		String name = "Pegger Fisher"; // Strings are actually arrays of characters

	}
}
